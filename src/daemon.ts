// Build-only placeholder. Task 8 replaces this with the daemon entrypoint.
// Keeping it inert prevents Task 3 from speculatively implementing lifecycle,
// transports, intake, or service behavior owned by later plan tasks.
export {};
